services:
  reader:
    build: .
    container_name: reader
    ports:
      - "8320:8320"
    env_file:
      - path: .env
        required: false   # no secrets needed since the RSS switch
    volumes:
      - ./data:/data
    restart: unless-stopped
